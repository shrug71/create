<!--<h1>Create New Order</h1>

<%= form_with(model: @orders, url: orders_path, method: :post) do |form| %>
  <table class="table table-hover" id="cafe-products-list">
    <thead>
      <tr>
        <th>Name</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Total Amount</th>
      </tr>
    </thead>
    <tbody>
      <%# @menus.each do |menu| %>
        <tr class="row-select cart">
          <td class="get-menu-name">
            <%#= menu.name %>
          </td>
          <td class="get-menu-price price">
            <%#= menu.price %>
          </td>
          <td>
            <%#= form.number_field "menus_attributes[][quantity]", class: 'form-control qty', value: 0, data: { menu_price: menu.price } %>
          </td>
          <td>
            <span class="subtotal">0</span>
            <%#= form.hidden_field "menus_attributes[][subtotal]", class: 'subTotalInput' %>
          </td>
          <td>
            <%#= form.hidden_field "menus_attributes[][menu_id]", value: menu.id %>
          </td>
        </tr>
      <%#end %>
    </tbody>
  </table>

  <div class="actions">
    <%#= form.submit "Place Order", class: 'btn btn-primary' %>
  </div>
<%# end %>-->

<h1>Order Status<h1>
<%= render "form", order: @order %>